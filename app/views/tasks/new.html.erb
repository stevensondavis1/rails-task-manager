<div class="container">
  <h1>Add a Task</h1>

  <%= simple_form_for @task do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>
    <%= f.input :completed, as: :boolean %>
    <%= f.submit 'Create Task', class: 'btn btn-primary' %>
  <% end %>
</div>
